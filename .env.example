# Provider selection (together | openrouter)
OCR_PROVIDER=together

# ===== Together AI =====
# API key for Together (required if OCR_PROVIDER=together)
TOGETHER_AI_API_KEY=your_together_api_key
# Optional: default vision model
# TOGETHER_VISION_MODEL=meta-llama/Llama-4-Scout-17B-16E-Instruct

# OPENROUTER_API_KEY=your_openrouter_api_key

# Optional: recommended vision model for OpenRouter
# OPENROUTER_MODEL=openai/gpt-4o-mini